// Ensure the environment variable is correctly prefixed for client-side usage
const baseUrl = process.env.REACT_APP_BASE_URL; // Corrected the prefix
// const apiKey = process.env.REACT_APP_API_KEY;

// Export the variables
export default baseUrl;
// export { apiKey };
